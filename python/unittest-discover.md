# Run Python tests (`unittest`) in a particular folder

A way to run Python's `unittest` tests in files with the pattern `test_*.py`

```sh
python -m unittest discover -s /path/to/folder -p 'test_*.py'
```
